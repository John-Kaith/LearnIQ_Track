/**
 * LearnIQ brand colors — gold/amber light theme, matching the web frontend's
 * light mode. Re-exports the already-built light palette from
 * `learniqTheme.ts` (the fuller design-system file) so there's one source of
 * truth instead of two hardcoded palettes that can drift out of sync.
 */
export { Colors } from '@/constants/learniqTheme';
