/**
 * Copy to learniq-config.js and set your Ubuntu laptop API URL.
 * Windows dev: use the Ubuntu machine LAN IP (ip addr / hostname -I).
 *
 *   cp js/learniq-config.example.js js/learniq-config.js
 */
window.LEARNIQ_CONFIG = {
  /** Example: http://192.168.1.50:8000 — leave empty to use localStorage only */
  apiBase: "",
};
